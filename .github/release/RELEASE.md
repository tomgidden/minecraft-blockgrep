**Minecraft 26.x | Fabric**

Highlights every occurrence of a block pattern near you. Patterns are built from per-cell predicates (block ids, tags, alternations, wildcards and negations)

- `blockgrep-fabric-${VERSION}.jar` — Fabric
